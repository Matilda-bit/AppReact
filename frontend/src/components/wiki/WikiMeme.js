import React from 'react';
import classes from './PageContent.module.css';

function WikiMeme() {
  return (
     <div className={classes.wikiMeme}>
      <h1>{title}</h1>
    </div>
  );
}

export default WikiMeme;